import React from 'react'

const DriverProfile = () => {
    return (
        <div>
            {/* List of assigned orders with delivery details (customer name, fuel type, location).
            Driver Detail-Name,Address,Phone NO, Aadhar number
            Buttons to update status: "En Route," "Delivered," etc. */}
        </div>
    )
}

export default DriverProfile